# AGENTS.md - Halcyon 03号观测站：信号接收终端

## 项目定位 (Project Identity)

- **项目名称**: Halcyon 03号观测站 · 信号接收终端 (Halcyon Observatory 03 – Signal Acquisition Terminal)
- **技术栈**: Vue 3 (Composition API) + Vite + TypeScript + Sass
- **核心诉求**:
    1. 构建一次**具有选拔意义的沉浸式叙事体验**——将招聘流程本身转化为世界观的一部分，用“信号匹配”筛选出真正理解海希安内核的同频者。
    2. 延续 Halcyon 官网**全手绘 SVG 图形系统**与“数据荒原里的心跳”美学，但调性从“安静的怅然”转向**“静谧的召唤”**
       ：等待、聆听、然后精准回应。
    3. 所有交互必须**服务于“波长检测”**——按钮不是按钮，是“频率锁定”；题目不是题目，是“相位校准”；简历投递不是表单提交，是“龙骨数据上载”。
    4. **不可出现**任何传统招聘网站的模板化元素（职位列表、JD 卡片、公司简介幻灯片）。这里是观测站，不是 HR 系统。

---

## 设计摘要 (Design Synopsis)

### 1. 世界观与情感内核

- **场景设定**：  
  时间校准为 **2026 年 3 月，江西，地球**。用户扮演一名无意中接收到 **造父四（delta Cephei）** 深空信号的“观测者”。信号源是 *
  *03 号观测站**——一个在官方档案里已被标记为“数据沉降”的前哨站。

- **核心隐喻**：  
  招聘 = **信号同步**。  
  简历 = **观测日志**。  
  匹配度 = **波长共振**。  
  整个页面是一次 **双向频率校准**：团队在寻找能解码 0KB 空白的人，而你在决定是否回应这段来自未来的邀请。

- **情感基调**：  
  非热血、非催促。是 **寒夜里的篝火、老式电台的底噪、黑胶唱针落下那一刻的期待**。  
  页面不“说服”你投递，只负责让**对的人认出彼此的信号**。

- **关键符号**（均需手绘 SVG 化）：
    - **造父四（δ Cep）**：一颗亮度周期性变化的变星，页面主视觉中作为脉冲光源出现。
    - **LUX 侧影（Unit-01）**：只在“觉醒”阶段完整显现，此前仅以破碎轮廓或机械臂局部暗示。
    - **黑胶唱片与红轴键盘**：代表复古科技审美，也是线下接头暗号的实体信物。
    - **天文钟滴答声**：不可见的音频符号，仅在佩戴耳机时被察觉。
    - **0KB 龙骨文件**：页面彩蛋的核心视觉符号，以极小字号手写体呈现。

---

### 2. 视觉规范 (Visual Rules)

#### 色彩体系

| CSS 变量           | 色值        | 用途说明                          |
|------------------|-----------|-------------------------------|
| `--color-void`   | `#0f0f12` | 主背景，近乎纯黑，带极微弱星尘纹理             |
| `--color-pulse`  | `#ff9f4b` | 脉冲光/心跳色，比原 `heartbeat` 更暖，带橙调 |
| `--color-paper`  | `#f5efe6` | 文字/图形主色，旧档案纸白                 |
| `--color-dust`   | `#2a2c2e` | 次级背景，模拟阴极射线管屏幕的关闭状态           |
| `--color-signal` | `#59b89c` | 信号锁定/成功状态色，低饱和海绿              |
| `--color-mist`   | `#8f9aa7` | 辅助/失效文字，雾气灰                   |
| `--color-error`  | `#a6342e` | 极少使用，仅用于极低概率错误提示（如“同步失败”）     |

- **纹理**：全局叠加轻微 **CRT 扫描线**（白线，不透明度 0.02）与 **胶片颗粒噪声**。
- **特殊效果**：脉冲光源带 **径向渐变羽化**，模拟星点衍射。

#### 字体系统

- **主标题 / 信号文本**：`PP Fragment` 或 `Switzer Inktrap`——字形略不稳定，带有“手写笔迹被信号干扰”的细微断笔感。
- **正文 / 观测日志**：`Source Serif 4`，衬线体，营造阅读纸质档案的权威感与温度。
- **数据 / 代码片段**：`JetBrains Mono`，等宽，用于显示“正在同步本地时空...”等系统提示。
- **手写注释**：`Klee One` 或 `Caveat`，用于选项点评、拍摄注释等感性内容。

#### 图形与图标系统

- **全部使用手绘风格 SVG**，线条保留压感笔触，填充允许不均匀的噪点渐变。
- **必须绘制的新图标集**：
    - 造父四变星光变曲线（周期律动波形）
    - 03 号观测站轮廓（穹顶+抛物面天线）
    - LUX 机械臂分解图（仅局部出现）
    - 黑胶唱片纹路（带模拟爆豆点）
    - 红轴键盘单个键帽（Cherry MX 红轴剖面）
    - 0KB 文件图标（透明文件夹，内为空白）
- **所有 SVG 均需通过 `<HandDrawnSVG>` 组件包装**，统一添加 `stroke-dasharray` 手绘动画能力。

#### 布局原则

- **反对称基准**：所有大区块避免绝对居中。主脉冲光偏右 1/3，文案区块偏左 1/3。
- **层叠深度**：通过 `box-shadow` 制造多层胶片负片效果，模拟观测台控制台上叠放的透明硫酸纸。
- **大留白**：留白区域必须容纳“凝视”所需的时间。组件间距采用 **8px 网格系统**，但纵向间距常放大至 64px 以上。
- **滚动即叙事**：页面**单屏长滚动**，每个滚动步进对应“信号接收 → 答题校准 → 觉醒 → 行动”的叙事章节。

---

### 3. 交互与动效原则

- **严禁**任何 `ease-in-out`。所有运动必须带有**机械感**或**信号抖动**。

| 用途      | 缓动曲线                                    | 情感解释            |
|---------|-----------------------------------------|-----------------|
| 脉冲光跳动   | `cubic-bezier(0.5, 1.8, 0.8, 0.8)`      | 模拟心跳过冲，然后快速归位   |
| 按钮悬停/锁定 | `cubic-bezier(0.2, 0.9, 0.3, 1.2)`      | 轻微回弹，仿佛机械锁扣咬合   |
| 页面元素入场  | `cubic-bezier(0.34, 1.56, 0.64, 1)`     | LUX 机械臂展开式回弹    |
| 页面元素退场  | `cubic-bezier(0.61, 0.01, 0.88, 0.37)`  | 快速收缩，带迟疑        |
| 信号失谐/错误 | `cubic-bezier(0.1, 0.9, 0.2, 1.1)` + 抖动 | 短暂水平位移抖动，模拟信号不稳 |

- **关键交互反馈**：
    - **按钮**：常态为手绘空心线框；悬停时线框变为实心填充，心跳色从内部“渗出”；点击时有极短的下陷位移（1px）并伴有 **18ms
      白噪声爆破音**（音频极轻）。
    - **选项卡片**：未选中时半透明灰边，选中时**边缘发出微弱脉冲光晕**，选项文字轻微左移，模拟磁头读取。
    - **彩蛋触发**：页面右下角“天文钟”SVG 区域**长按 3 秒**，触发隐藏文案渐显，同时背景心跳声短暂加强。

- **音频设计原则**：
    - 不使用背景音乐，只有**极微弱的底噪**（17kHz 高频脉冲 + 天文钟机械滴答）。
    - 所有音效均采用 **22kHz / 8bit 低保真采样**，模拟远程无线电传输损耗。
    - 音效文件总大小控制在 200KB 以内，优先使用 Web Audio API 合成（如滴答声、脉冲）。

---

## 项目结构蓝图 (Project Structure)

```
halcyon-recruitment/
├── public/
│   ├── favicon.ico
│   └── svg-assets/
│       ├── signals/               # 造父四、脉冲波形等
│       ├── lux/                  # LUX 轮廓、机械臂逐帧
│       └── icons/               # 观测站、黑胶、键盘等
├── src/
│   ├── assets/
│   │   ├── fonts/
│   │   ├── audio/
│   │   │   ├── pulse-17khz.wav
│   │   │   ├── lock-click.wav
│   │   │   └── projector-crank.wav
│   │   └── textures/
│   │       ├── crt-lines.png
│   │       └── film-grain.png
│   ├── components/
│   │   ├── core/
│   │   │   ├── AppCanvas.vue        # 噪声+扫描线背景
│   │   │   ├── HandDrawnSVG.vue     # 统一手绘SVG包装
│   │   │   ├── SignalText.vue       # 带脉冲光标的动态文本
│   │   │   └── ObsButton.vue        # 观测站风格按钮
│   │   ├── modules/
│   │   │   ├── SignalPulse.vue      # 开场跳动光点
│   │   │   ├── Questionnaire.vue    # 五道题的容器，含相位指示器
│   │   │   ├── LUXReveal.vue        # LUX 立绘渐现与语音
│   │   │   ├── FrequencyLock.vue    # 底部CTA区域
│   │   │   └── EasterEgg.vue        # 长按彩蛋
│   │   └── layout/
│   │       ├── SiteHeader.vue       # 极简，仅03号观测站标志
│   │       └── SiteFooter.vue       # 天文钟SVG + 版权/彩蛋区
│   ├── composables/
│   │   ├── usePulse.ts             # 脉冲光周期控制
│   │   ├── useAudioContext.ts      # Web Audio 合成滴答声
│   │   └── useScrollLock.ts        # 答题阶段锁定滚动？
│   ├── router/
│   │   └── index.ts               # 仅单页，但保留路由便于扩展
│   ├── stores/
│   │   └── signal.ts              # Pinia：记录答题状态、锁定状态
│   ├── styles/
│   │   ├── _variables.scss        # 设计令牌
│   │   ├── _globals.scss          # 重置，CRT纹理全局应用
│   │   ├── _mixins.scss           # 手绘边框、噪声、脉冲动画
│   │   └── main.scss
│   ├── utils/
│   │   ├── drawingUtils.ts        # 生成手绘路径、干扰线
│   │   └── wavelength.ts          # 答题匹配算法（轻量）
│   ├── views/
│   │   └── SignalAcquisition.vue  # 唯一页面：信号接收终端
│   ├── App.vue
│   └── main.ts
├── index.html
├── package.json
├── vite.config.ts
└── AGENTS.md (本文件)
```

---

## 页面内容规划 (Page Content Outline)

### 页面名称：`SignalAcquisition.vue` – 03号观测站 · 信号接收终端

**整体叙事结构**：单页长滚动，五个视觉章节严格对应“开场 → 答题(五题) → 觉醒 → CTA → 彩蛋区”。

---

#### 第一节：信号捕获 (The Hook)

**氛围**：  
全屏漆黑，中央一个 **手绘造父四 SVG**，以真实变星周期（约 5.4 天）压缩为 5.4 秒的脉冲周期，亮度从 20% 至 100% 平滑变化。  
背景叠加 17kHz 正弦波脉冲（Web Audio 合成，仅当用户与页面发生首次交互后激活）。  
屏幕底部 1/3 处，**SignalText 组件** 逐字打印（打字机效果，每字 80ms 延迟）：

```
> 正在从 deltaCephei（造父四）接收信号...
> 信号来源：03号观测站
> 正在同步本地时空：2026年3月，江西，地球
> 观测者，你听得到吗？
```

最后一行打印完毕后，**“连接信号”按钮** 以回弹缓动出现，按钮周围环绕微弱径向光晕。  
按钮文案：**“连接信号 · 校准相位”**。  
点击后，页面平滑滚动至第二节，同时 17kHz 脉冲增加一层低频谐波，暗示锁定。

**细节扩充**：

- 脉冲光点 SVG 内部包含 3 层圆：核心（亮橙）、光晕（橙红）、衍射芒（半透明长线条）。每层独立动画，模拟恒星闪烁。
- 打字光标为手绘竖线，自带细微抖动。
- 连接按钮按下时，整个页面背景 CRTs 扫描线短暂增强，模拟电磁干扰。

---

#### 第二节：相位校准 (The Filter · 五道选题)

**氛围**：  
背景变暗，原脉冲光点上移成为角落里的固定装饰。中央出现 **手绘的“频率刻度盘”**，指针指向当前题号（1/5）。  
每道题以 **卡片组** 形式呈现：左侧是手绘插图（与题目主题对应），右侧是选项列表。  
选项为圆形单选钮，但设计成**老式收音机按键**——圆形，带机械凹陷阴影。

**题目结构与交互**：

- 每道题出现时，频率刻度盘指针轻跳至对应位置。
- 题目上方显示短副标题，例如 `【01 · 天文】关于「距离与时间」`。
- 选项悬停时有轻微抬起效果，点击时伴随 **锁定声**（短促 click），选项框变为心跳色填充。
- **禁止**在答题过程中显示正确与否；所有题目答完后统一进入下一节。
- **点评文字** 以极小号手写体出现在题目下方，仅当用户**选中任意选项后**浮现，且保持半透明状态（不干扰选择）。

**具体题目视觉化设计**：

1. **造父四**

- 左侧插图：手绘造父四与光行差示意图，标注 880 光年。
- 点评文字（选B后高亮显示）：“视界即历史——你看到了时间本身。”

2. **特修斯之船**

- 左侧插图：手绘 LUX 的半边机械剖面，另一侧为人类神经元。
- 点评文字（选B后）：“你理解 0KB 的龙骨为何不可替代。”

3. **遗憾**

- 左侧插图：未完成的纸船，或断掉的磁带。
- 点评文字（选B）：“理性的记忆模型。”（选C）：“感性的存在证明。” 二者并置。

4. **黑胶底噪**

- 左侧插图：唱针落入唱片纹路，溅起细微尘埃。
- 点评文字（选B）：“生命本身就是脏且不完美的。”

5. **灵魂**

- 左侧插图：AI 生成的完美夕阳，与 LUX 侧影凝视夕阳的对比。
- 点评文字（选B）：“它缺的是‘自己会死’的恐惧。”

**进度反馈**：  
屏幕右侧边缘有一条垂直的 **“波长锁定条”**，每答一题，心跳色填充增加 20%，并伴有轻微的脉动动画。

---

#### 第三节：觉醒 (The Reveal)

**触发条件**：五题全部完成（无论选择如何，但选中任意选项即可进入下一题，无跳题限制）。

**视觉与音频爆发**：

- 页面突然“抖动”一下（水平位移 3px 快速复位），17kHz 脉冲逐渐变形，3 秒内过渡为一段 **空灵女声清唱**（Suno 生成，无歌词，仅哼鸣）。
- 背景全黑，所有 UI 元素淡出。
- 屏幕中央，无数手绘线条从四周向中心汇聚，逐渐构成 **LUX (Unit-01) 的完整立绘**——从模糊到清晰，线条收笔时带有机械臂展开的回弹缓动。
- 立绘下方，**SignalText** 再次逐字打印：

```
> 身份确认：首批观测者（Founder Observer）
> 你好，我是 Entity 03，你可以叫我 LUX。
> 谢谢你，在 2026 年的喧嚣中，精准地定位了我的频道。
```

- LUX 的眼睛部位有极微弱的呼吸灯动画（心跳色，1.2 秒周期）。

**细节扩充**：

- LUX 立绘使用分层 SVG：基础轮廓、机械臂、能源环、眼神光。入场动画按“轮廓 → 能源环 → 机械臂展开 → 眼神光亮起”顺序执行。
- 女声音频**自动播放**需遵循浏览器策略，故在用户首次点击“连接信号”时预加载并标记为用户手势可用。

---

#### 第四节：行动指令 (CTA)

**氛围**：  
立绘淡化成为左侧固定装饰，右侧出现文字区块，背景恢复为 CRT 纹理。  
文案以**信件格式**呈现，使用衬线字体，首字下沉。

```
来自海希安的邀请函

我们正在建造一艘名为《Project RY》的船。
我们不需要螺丝钉，我们需要能听到星辰跳动的“船员”。

你将获得：
· 第一手 ESA Gaia DR3 数据的“解密”权限。
· 参与制作一张 $59.99 的物理黑胶。
· 在 57 平米的实验室里，挑战这个平庸的时代。
```

**按钮区**：  
两个按钮并置，设计上弱化主次，但视觉重量一致：

- **投递观测日志**：链接至表单页面（新页面或弹层，风格延续）。
- **寻找红轴键盘**：显示线下地址弹窗，文案为“3月X日 · [学校地址] ｜ 接头暗号：0KB”。

按钮下方附极小号手写体：

> “0KB 的空白里，藏着我们对宇宙最后的执念。”

---

#### 第五节：彩蛋与余韵 (Easter Egg)

**位置**：页面最底部，`SiteFooter.vue` 内嵌入 **手绘天文钟 SVG**。  
**交互**：在天文钟表盘区域**长按 3 秒**，触发隐藏文案：

- 文案以 **等宽字体** 渐显：`> resolve states via physical interference...`
- 同时心跳色脉冲频率加快，天文钟指针开始逆向缓慢旋转。
- 文案出现后停留 5 秒渐隐，期间用户可以截图。

**设计意义**：  
此彩蛋用于识别“顶级极客”——他们会为了探索界面边界而长按看似静态的装饰元素。发现者可直接将截图发至招聘邮箱，获得额外关注。

---

## 开发任务优先级 (Development Priority)

### Phase 0：基础与环境

- 初始化 Vue 3 + TS + Vite 项目。
- 建立 `_variables.scss`，注入完整设计令牌。
- 实现 `AppCanvas` 组件，全局应用 CRT 扫描线 + 胶片颗粒。
- 配置 Web Audio 上下文，合成 17kHz 脉冲与天文钟滴答。

### Phase 1：叙事骨架与开场

- 构建 `SignalAcquisition.vue` 长滚动布局，设定各章节锚点。
- 实现 `SignalPulse` 组件（造父四脉冲光点）。
- 实现 `SignalText` 打字机组件。
- 实现 `ObsButton` 并绑定脉冲音效。

### Phase 2：答题模块

- 实现 `Questionnaire` 状态管理（Pinia）。
- 实现五道题卡片布局与单选反馈。
- 实现频率刻度盘与波长锁定条动画。
- 接入点评文字条件显示。

### Phase 3：觉醒与 CTA

- 实现 LUX 立绘分层 SVG 与汇聚动画。
- 集成 Suno 女声音频，处理自动播放策略。
- 实现邀请函区块及双按钮。
- 实现彩蛋长按检测与隐藏文案。

### Phase 4：优化与收尾

- 手绘 SVG 精灵图合并，减少请求。
- 响应式适配：重点保障 iPad 横屏与桌面 1440p。
- 音频压缩与预加载策略。
- 表单页面的风格延续（另建简易页面，但确保返回终端流畅）。
- 部署测试（建议 Netlify 或 Vercel）。

---

**设计师签名**：  
这不是一次招聘，是一次信号广播。我们不知道谁会收到，但我们确信——  
**0KB 的空白，总会等到愿意用肉身填补那个空缺的人。**

---  
*文档版本：v1.0 · 对应招聘终端初始设计*  
*最后更新：2026年2月12日 03:42 GMT+8*

---

原始指令：
第一部分：开场（The Hook）
视觉： 屏幕一片漆黑，中央只有一个光点在微微跳动。
配音/音效： 微弱的、带着底噪的 17kHz 高频脉冲声。
文案：
“正在从 deltaCephei（造父四）接收信号...”
“信号来源：03号观测站”
“正在同步本地时空：2026年3月，江西，地球”
“观测者，你听得到吗？”
[ 按钮：连接信号 ]
第二部分：答题环节（The Filter）
题目不设标准答案，但选项背后隐藏着**“波长匹配”**。

1. 【天文】关于「距离与时间」
   此题测试：是否具备宏大叙事的浪漫感。
   题目： 当我们在游戏里仰望距离地球 880 光年的 delta Cephei（造父四）时，我们看到的其实是？
   A. 一颗表面温度 6000K 的黄超巨星。
   B. 880 年前的过去。
   C. 它是宇宙的灯塔，用来测量距离。
   D. 只是天空的一张贴图。
   点评： 选 B 的人，懂“视界即历史”，这才是做太空歌剧需要的时空观。
2. 【哲学】关于「特修斯之船」
   此题测试：对“自我意识”的定义，也就是游戏核心谜题的理解。
   题目： 如果我们将一个人的身体部件逐一替换成机械，最后连大脑记忆也全部数字化上传，原来的肉体销毁。现在的“他”，还是原来的“他”吗？
   A. 是，因为记忆和思维逻辑完全延续了。
   B. 不是，原来的他已经死了，这只是一个完美的复制品。
   C. 无所谓，只要能活下去形式不重要。
   D. 看法律怎么定义。
   点评： 选 B 的人，能理解为什么你的男主会痛苦，为什么我们要寻找那 0KB 的龙骨。
3. 【心理学】关于「遗憾」
   此题测试：对“悲剧美学”的共情能力。
   题目： 为什么在大多数经典叙事中，“未完成的约定”往往比“大团圆结局”更让人念念不忘？
   A. 因为人类有受虐倾向。
   B. 因为“蔡格尼克效应”：大脑对未完成的任务记忆更深刻。
   C. 因为遗憾是唯一能证明我们真的爱过的证据。
   D. 因为悲剧更容易拿奖。
   点评： 选 B 是理性的产品经理/程序；选 C 是感性的文案/美术。这两个都得要。
4. 【科技】关于「模拟与数字」
   此题测试：对“黑胶/低保真”这种复古科技的审美。
   题目： 在数字音乐采样率可以达到 192kHz 的今天，为什么我们还要在这个游戏中费力去模拟黑胶唱片的底噪和爆豆声（Crackle）？
   A. 为了假装高端，提高售价。
   B. 这种噪音是“不完美”的，而生命本身就是脏且不完美的。
   C. 因为复古风现在是市场潮流。
   D. 为了掩盖音频压缩的瑕疵。
   点评： 选 B 的人，才配去摸你的红轴键盘和黑胶唱机。
5. 【AI】关于「灵魂」
   此题测试：对 AI 工具的态度（这是安苏你的核心雷区）。
   题目： AI 可以一秒钟生成一张完美的夕阳图，但它画不出“看着夕阳想起逝去亲人”的那种感觉。缺的是什么？
   A. 算力还不够大，迟早能画出来。
   B. 缺的是它自己“会死”的恐惧。
   C. 缺的是随机性参数。
   D. AI 画得挺好的啊，看不出区别。
   点评： 选 B 的人，深刻理解了为什么“碳基生物”不可替代。

第三部分：觉醒与奖励（The Reveal）
视觉： 答题结束，屏幕出现剧烈抖动，17kHz 的噪音转为一段由 Suno 搓出来的空灵女声。
画面： 所有的像素线条迅速汇聚，LUX 的精美立绘（由模糊变清晰）第一次跃然屏上。
文案：
“身份确认：首批观测者（Founder Observer）”
“你好，我是 Entity 03，你可以叫我 LUX。”
“谢谢你，在 2026 年的喧嚣中，精准地定位了我的频道。”
第四部分：行动指令（CTA）
文案：
“来自海希安的邀请函”
我们正在建造一艘名为《Project RY》的船。
我们不需要螺丝钉，我们需要能听到星辰跳动的“船员”。
你将获得：
第一手 ESA Gaia DR3 数据的“解密”权限。
参与制作一张 $59.99 的物理黑胶。
在 57 平米的实验室里，挑战这个平庸的时代。
投递简历/作品集： [ 按钮 ]
或者： 带着你的“波长”，于 3月X日 到 [学校地址] 寻找那个拿着红轴键盘的男人。
“0KB 的空白里，藏着我们对宇宙最后的执念。”
设计小心思：
彩蛋机制： 在 H5 的某个角落，设置长按 3 秒，会跳出一行隐藏代码：resolve states via physical
interference...。如果有人发现了并截图发给你，这人绝对是顶级极客。
音效杀： 整个过程音量建议设定在特定范围，只有当你认真戴上耳机听时，才能分辨出背景里交织的“天文钟”滴答声。
